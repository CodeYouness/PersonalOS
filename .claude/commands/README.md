# Project commands

Slash commands for this repository, one markdown file per command.

Empty for the same reason as `../skills/`: the workflow in
`../../docs/workflow.md` is followed by hand first, and only the parts that
turn out to repeat get a file.
